/**
 * Created by linyi on 16/8/15.
 */
var winston =require('winston');

console.log(winston.loggers);
winston.info("hello world");

var logger=new (winston.Logger)({
    transports:[new (winston.transports.Console)({
        json:true
    })]
});

logger.info("test console json");
logger.info({a:1,b:2});


winston.remove(winston.transports.Console);
winston.add(winston.transports.Console,{json:true,logstash:true});
winston.info("winston.loggers",winston.loggers);